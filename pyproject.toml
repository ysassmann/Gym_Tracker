[project]
name = "gym-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.31",
    "python-dotenv>=1.0",
    "psycopg[binary]>=3.3.3",
    "streamlit-cookies-controller>=0.0.4",
]

[tool.uv]
package = false
